package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Elegant Light / White Theme Color Palette (as requested)
val ElegantLightBg = Color(0xFFFAFAFA)         // Crisp light pristine background
val ElegantLightSurface = Color(0xFFFFFFFF)    // Pure white for sections/cards
val ElegantLightContainer = Color(0xFFF1F5F9)  // Modern grey-blue background for fields and minor panels
val ElegantLightBorder = Color(0xFFE2E8F0)     // Soft light borders

// Brand Colors (derived from the QC Auditing Logo)
val BrandNavy = Color(0xFF0D253F)             // Corporate Primary Navy Blue
val BrandTeal = Color(0xFF00BFA5)             // Brand Accent Teal
val BrandNavyMuted = Color(0xFF1E3A5A)        // Muted Primary
val BrandTealMuted = Color(0xFF009688)        // Muted Accent Teal

// Text Colors
val ElegantDarkText = Color(0xFF0F172A)        // Slate-900 (deep charcoal) for maximum legibility
val ElegantMutedText = Color(0xFF64748B)       // Slate-500 for captions, helper text

// High-Contrast Light Mode Status Colors (Safe, highly accessible compliance indicators)
val ElegantLightGreen = Color(0xFF2E7D32)      // Success compliance green (Closed / OBS low risk)
val ElegantLightOrange = Color(0xFFE65100)     // Alert orange (Open / Minor NCR)
val ElegantLightRed = Color(0xFFC62828)        // Serious error red (Major NCR)


